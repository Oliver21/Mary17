
program polaco:
	int ia;
	int ic;
	int id;
	float ie;
	int in;
	int ij;
	int ik;
	int ip;
	int iz;
	int ix;
	int itotal;
	bool buno;
	bool bdos;
	int iBarney (int iQue, string sPaso, char cAmigito){	
		ia = ij + 5.5;
	}
{
	bdos = buno && (itotal == (ic + id + ie + in + ij * ik + ip - iz / ix) + ic);
	//ia = ic != id;
	bdos = buno == bdos;
	ia = 4.3 * 1 + 9 + 8 * 2 / 3 + 1 * 5 * (5 + 1 + 5 - 3) + 1;
	print ("d");
}
