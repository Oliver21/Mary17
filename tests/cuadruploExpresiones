program polaco:
	int ia;
	int ic;
	int id;
	float ie;
	int in;
	int ij;
	int ik;
	int ip;
	int iz;
	int ix;
	int itotal;
	bool buno;
	bool bdos;
	int iBarney (int iQue, string sPaso, char cAmigito){
		int ij;	
		ia = ij + 5.5;
	}
{
	ia = buno && (itotal == (ic + id + ie + in + ij * ik + ip - iz / ix) + ic);
	//ia = ic != id;
	ic = buno == bdos;
	ia = 4.3 * 1 + 9 + 8 * 2 / 3 + 1 * 5 * (5 + 1 + 5 - 3) + 1;
}
