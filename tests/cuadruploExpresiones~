
program polaco:
	int ia;
	int ic;
	int id;
	float ie;
	int in;
	int ij;
	int ik;
	int ip;
	int iz;
	int ix;
	int itotal;
	bool buno;
	bool bdos;
	int iBarney (int iQue, string sPaso, char cAmigito){
		int ij;	
		ij = ij + 5.5;
	}
{
	bdos = buno && (itotal == (ic + id + ie + in + ij * ik + ip - iz / ix) + ic);
	//ia = ic != id;
	bdos = buno == bdos;
	ia = 4.3 * 1 + 9 + 8 * 2 / 3 + 1 * 5 * (5 + 1 + 5 - 3) + 1;
	if(4 < 5){
	ia = 10 + 1;
	ic = 4;
	print("Esto esta dentro del ciclo if");
	} else {
	ia = 5 + 5;
	print("Esto es parte del else");
	}
	print ("Esta linea esta despues del IF");
}
